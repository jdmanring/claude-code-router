.DS_Store
node_modules
dist
release
*.log
.env
.env.*
!.env.example
.idea
.claude
.bot-gateway-state
.agent-data
.agent-console-remote-mode
tmp
release-local
logs
.opencat
test-results
playwright-report
blob-report
.tmp
.test-dist

# local build artifact, never commit
*.appimage
docker/local-ai-gateway/*.tgz
packages/*/*.tgz

# local code-graph and LSP indexes, regenerated per machine
graphify-out
.serena

# local binary-analysis ledger, keyed by each artifact's hash
.reverify

# private proposal drafts, kept out of the published fork
proposals/
